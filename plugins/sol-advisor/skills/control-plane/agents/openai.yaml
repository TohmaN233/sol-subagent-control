interface:
  display_name: "Sol Subagent Control"
  short_description: "Run versioned Workflows with pinned Providers and explicit main acceptance"
  default_prompt: "Use $sol-control-plane; read metadata once, run a pinned Workflow, and verify actual evidence."
